# トレーニングメニュー作成アプリケーション

PDFファイルから必要なページを選択して、トレーニングメニュー用のPDFを作成するアプリケーションです。

## 機能

- カレントディレクトリのPDFファイル一覧を表示
- PDFファイルのプレビュー表示
- ページごとのサムネイル表示
- 複数のPDFファイルから任意のページを選択
- 選択したページを結合して新しいPDFを作成

## 必要要件

### 開発環境での実行

- Python 3.8 以上
- 必要なライブラリ（requirements.txtに記載）

### 追加要件（Linux/Mac）

pdf2imageを使用するため、poppler-utilsが必要です：

```bash
# Ubuntu/Debian
sudo apt-get install poppler-utils

# macOS
brew install poppler
```

## インストール方法

### 1. リポジトリのクローン

```bash
git clone <repository-url>
cd <repository-directory>
```

### 2. 依存関係のインストール

```bash
pip install -r requirements.txt
```

## 使い方

### 開発環境で実行

```bash
python training_menu_app.py
```

### EXEファイルの作成（Windows用）

```bash
python build_exe.py
```

実行ファイルは `dist/TrainingMenuApp.exe` に作成されます。

## アプリケーションの使い方

### 1. PDFファイルの準備

- 開発環境: スクリプトと同じディレクトリにPDFファイルを配置
- EXE実行時: EXEファイルと同じディレクトリにPDFファイルを配置

### 2. 基本操作

1. **PDFファイルの選択**
   - 左側のリストからPDFファイルを選択
   - 選択すると、中央にプレビューとページ一覧が表示されます

2. **ページの選択**
   - 下部のサムネイル一覧から、必要なページを確認
   - サムネイルをクリックすると、上部のプレビューが更新されます
   - 「印刷エリアに追加」ボタンで右側の印刷エリアに追加

3. **印刷とPDF保存**
   - 右側の印刷エリアで選択したページを確認
   - 不要なページは「削除」ボタンで削除可能
   - **「印刷」ボタン**: 選択したページをプリンターで直接印刷
     - 一時PDFファイルを自動作成し、印刷後に自動削除されます
     - Windows/Mac/Linux対応
   - **「PDF保存」ボタン**: 選択したページを新しいPDFファイルとして保存
     - 保存先とファイル名を指定できます
     - デフォルト名は `training_menu.pdf`

## UI構成

```
+------------------+-------------------------+------------------+
|  PDFファイル一覧 |      プレビュー          |   印刷エリア     |
|  (縦リスト)      |                         |                 |
|                  +-------------------------+                 |
|                  |   ページサムネイル       |                 |
|                  |   (横スクロール)         |   [クリア]      |
|                  |                         |   [印刷][PDF保存]|
+------------------+-------------------------+------------------+
```

## トラブルシューティング

### PDFが表示されない

- PDFファイルが正しいディレクトリに配置されているか確認
- PDFファイルが破損していないか確認

### EXE作成時のエラー

- PyInstallerが正しくインストールされているか確認
- 管理者権限で実行してみる

### Linux/Macでサムネイルが表示されない

- poppler-utilsがインストールされているか確認
- `convert_from_path` の実行時にエラーが出ていないか確認

### 印刷できない

- **Linux/Mac**: `lpr`コマンドが利用可能か確認してください
  - プリンターが正しく設定されているか確認
  - CUPSがインストールされ、実行されているか確認
- **Windows**: デフォルトプリンターが設定されているか確認
- 印刷ダイアログが表示されない場合は、PDF保存機能を使用してファイルを保存してから手動で印刷してください

## ライセンス

このプロジェクトのライセンスについては、プロジェクトのルートディレクトリを参照してください。

## 開発者向け情報

### プロジェクト構成

```
.
├── training_menu_app.py    # メインアプリケーション
├── requirements.txt        # 依存ライブラリ
├── build_exe.py           # EXEビルドスクリプト
└── TRAINING_MENU_README.md # このファイル
```

### 主要な依存ライブラリ

- **tkinter**: GUI構築（Python標準ライブラリ）
- **PyPDF2**: PDF読み込みと結合
- **pdf2image**: PDFをイメージに変換
- **Pillow**: 画像処理
- **PyInstaller**: EXE化

## 今後の改善案

- [ ] ドラッグ&ドロップでページの順序変更
- [ ] PDFファイルのフィルタリング機能
- [ ] 印刷設定のカスタマイズ（用紙サイズ、余白など）
- [ ] プリセット保存機能
- [ ] ページの回転機能
- [ ] 複数ページの一括選択
